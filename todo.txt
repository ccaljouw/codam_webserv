carlo:
	add header class

	badrequest body size check
	root dingen defaults.
	post redirect


	request:
		request host
		test protocol check (not working as expected)

	response:
		checks before sending (status code set, stc)
		check WHITE_SPACE
		check capitalseensitivity
		check duplicate headers

	uri:
		add errors
		fix host / authority part 